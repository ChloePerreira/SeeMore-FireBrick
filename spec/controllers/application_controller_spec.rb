require 'rails_helper'

describe ApplicationController do
  describe "Session" do
    # it "session id should match user_id" do
    #   user = User.create!
    #   post :current_user=
    #   expect(session[:user_id]).to eq user.id
    # end
  end
end
