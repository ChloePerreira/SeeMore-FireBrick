FeedMe, I'm hungry!

<% if session[:user_id] %>
  Welcome, <%= User.find(session[:user_id]).name %>!&nbsp;
  <%= link_to "Log out", destroy_session_path, method: :delete %>
<% else %>
  <%= link_to "Log in with Vimeo", "/auth/vimeo" %>
<% end %>
